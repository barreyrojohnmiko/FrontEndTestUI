import { Box } from '@mui/material';

const HeaderView = () => {
  return (
    <Box sx={{ height: '50px', width: '100%', bgcolor: 'red' }}>
      <Box />
    </Box>
  );
};

export default HeaderView;
